# Core application
fastapi
uvicorn[standard]
python-multipart

# Music theory, notation and export
music21
pretty_midi

# Audio transcription
numpy
librosa
soundfile
onnxruntime
mir_eval
resampy

# basic-pitch pins tensorflow<2.15.1, which has no wheel for Python 3.11+.
# Its ONNX model is bundled in the wheel and onnxruntime alone can run it, so
# install it without dependencies:
#     pip install --no-deps basic-pitch==0.4.0
basic-pitch==0.4.0

# Hyphenation dictionaries for non-English lyrics (English uses built-in rules)
pyphen

# Tests
pytest
httpx          # FastAPI's TestClient
